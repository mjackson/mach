/**
 * The only feature required by default in the browser is
 * the client. Additional features can be loaded using e.g.
 *
 *   var mach = require('mach');
 *   require('mach/modules/features/accept');
 */
module.exports = require('./client');
